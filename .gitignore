.databricks/
build/
dist/
# deploy.sh rebuilds this wheel into app/backend/lib on every deploy (poetry build + cp) — it's a
# regenerated artifact, not source. The dist/ copy is already ignored; ignore the staged copy too.
modules/core/app/backend/lib/*.whl
__pycache__/
*.egg-info
.venv/
scratch/**
!scratch/README.md
.vscode
.cursor
.claude
**/dist/
poetry.lock
.DS_Store
*.gz
env.env
module.env
application.env
temp.*
/temp
mlruns
lightning_logs
.deployed
.idea
.dev
_dev
local/*
!local/README.md
