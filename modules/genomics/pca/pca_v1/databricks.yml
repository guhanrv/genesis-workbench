bundle:
  name: genesis_workbench_pca
  databricks_cli_version: ">=0.295.*"

include:
  - variables.yml
  - resources/*.yml

targets:
  prod_aws:
    mode: production
    default: true
    workspace:
      root_path: ${var.current_working_directory}
    permissions:
      - user_name: ${var.current_user}
        level: CAN_MANAGE
    run_as:
      user_name: ${var.current_user}
    resources:
      jobs:
        pca_initial_setup_job:
          job_clusters:
            - job_cluster_key: pca_setup_cluster
              new_cluster:
                aws_attributes:
                  availability: ON_DEMAND
        pca_compute:
          job_clusters:
            - job_cluster_key: pca_ingest_cluster
              new_cluster:
                aws_attributes:
                  availability: ON_DEMAND

  prod_azure:
    mode: production
    workspace:
      root_path: ${var.current_working_directory}
    permissions:
      - user_name: ${var.current_user}
        level: CAN_MANAGE
    run_as:
      user_name: ${var.current_user}
    resources:
      jobs:
        pca_initial_setup_job:
          job_clusters:
            - job_cluster_key: pca_setup_cluster
              new_cluster:
                azure_attributes:
                  availability: ON_DEMAND_AZURE
        pca_compute:
          job_clusters:
            - job_cluster_key: pca_ingest_cluster
              new_cluster:
                azure_attributes:
                  availability: ON_DEMAND_AZURE

  prod_gcp:
    mode: production
    workspace:
      root_path: ${var.current_working_directory}
    permissions:
      - user_name: ${var.current_user}
        level: CAN_MANAGE
    run_as:
      user_name: ${var.current_user}
    resources:
      jobs:
        pca_initial_setup_job:
          job_clusters:
            - job_cluster_key: pca_setup_cluster
              new_cluster:
                gcp_attributes:
                  availability: ON_DEMAND_GCP
        pca_compute:
          job_clusters:
            - job_cluster_key: pca_ingest_cluster
              new_cluster:
                gcp_attributes:
                  availability: ON_DEMAND_GCP
