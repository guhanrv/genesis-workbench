resources:
  volumes:
    pca_data_volume:
      catalog_name: ${var.core_catalog_name}
      schema_name: ${var.core_schema_name}
      name: pca_data
      volume_type: MANAGED

    pca_results_volume:
      catalog_name: ${var.core_catalog_name}
      schema_name: ${var.core_schema_name}
      name: pca_results
      volume_type: MANAGED
